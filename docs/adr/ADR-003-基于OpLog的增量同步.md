# ADR-003 基于 OpLog 的增量同步（预留）

- 状态：提议（未实现）
- 日期：2026-09-10

## 背景
未来云同步需要解决「离线修改后合并」的问题，简单全量覆盖会丢数据。

## 决策
- 采用操作日志（OpLog）思路：每次写操作记录（实体、操作、字段、时间戳、client_id）。
- 同步时按时间戳增量上传/拉取，冲突以「后写胜出 + 字段级合并」处理。
- 服务端只做存储与转发，不做业务计算。

## 后果
- 优点：天然支持离线、可回溯、冲突可审计。
- 代价：需要在服务层统一记录（依赖 ADR-005）；需要版本向量/时钟设计。
